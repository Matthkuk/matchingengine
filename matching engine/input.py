class Input:
    def __init__(self, input_id, user_id, order_type, instrument, quantity, btc, eth, usdt):
        self.input_id = input_id
        self.user_id = user_id
        self.order_type = order_type
        self.instrument = instrument
        self.quantity = quantity
        self.btc = btc
        self.eth = eth
        self.usdt = usdt